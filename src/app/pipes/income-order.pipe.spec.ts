import { IncomeOrderPipe } from './income-order.pipe';

describe('IncomeOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
